# Travel Journal (React)

A simple **travel journal application** built using **React** as part of the Scrimba React course.  
The project focuses on rendering **data-driven UI using props and reusable components**.

---

## 🚀 Tech Stack

- React
- JavaScript (ES6+)
- HTML & CSS
- Vite

---

## ✨ Features

- Displays a list of travel destinations
- Each entry shows:
  - Location image
  - Title and country
  - Travel dates
  - Description
  - Google Maps link
- Reusable `Card` component
- Data rendered dynamically from a JavaScript data array

---

## 🧠 What I Learned

- Passing data to components using **props**
- Rendering lists using `map()`
- Structuring a React app with reusable components
- Separating data from UI logic
- Using a single source of truth for rendering UI
- Setting up and running a React project with **Vite**

---

## 🛠️ Running the Project Locally

1. Clone the repository  
   ```bash
   git clone <repository-url>
